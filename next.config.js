/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    images: {
        domains: ['ucarecdn.com', 'cdn.iconscout.com', 'laravel.com', 'storage.googleapis.com'],
    },
}

module.exports = nextConfig
